rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /egresados/{imageId} {
      allow write: if request.auth != null; // O temporalmente if true para pruebas
      allow read: if true;
    }
  }
}